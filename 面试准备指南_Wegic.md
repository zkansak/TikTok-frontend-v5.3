# Wegic 前端岗位面试准备指南

## 📋 一、公司产品了解（必看）

### Wegic 产品特点
- **产品定位**：AI驱动的网站构建平台
- **核心功能**：帮助用户快速创建和管理网站
- **技术方向**：前端工具产品、Web浏览器端应用
- **岗位要求**：主要使用React框架，需要前端开发经验

### 面试前必做
1. ✅ 访问 https://wegic.ai/ 体验产品
2. ✅ 了解产品的主要功能和用户场景
3. ✅ 思考：如果让你来优化这个产品，你会怎么做？

---

## 🎯 二、技术栈准备重点（按优先级）

### ⭐⭐⭐ 必须熟练掌握（一面重点）

#### 1. JavaScript 核心
- **闭包（Closure）**
  - 定义：函数可以访问外部作用域的变量
  - 应用场景：模块化、私有变量、防抖节流
  - 面试题：实现一个防抖函数
  ```javascript
  function debounce(func, delay) {
    let timer;
    return function(...args) {
      clearTimeout(timer);
      timer = setTimeout(() => func.apply(this, args), delay);
    };
  }
  ```

- **this 指向**
  - 普通函数：指向调用者
  - 箭头函数：指向定义时的上下文
  - call/apply/bind 的区别

- **异步编程**
  - Promise：then/catch/finally
  - async/await：错误处理
  - 事件循环（Event Loop）：宏任务/微任务

- **ES6+ 特性**
  - 解构赋值、扩展运算符
  - 箭头函数、模板字符串
  - Map/Set、Symbol

#### 2. React 核心概念
- **组件生命周期**（函数组件）
  - useEffect：依赖数组、清理函数
  - useState：状态更新机制
  - useCallback/useMemo：性能优化

- **Hooks 深入理解**
  - useState：状态管理
  - useEffect：副作用处理
  - useCallback：函数缓存
  - useMemo：值缓存
  - useRef：DOM引用、持久化值

- **组件通信**
  - Props 传递
  - Context API
  - 状态提升

- **性能优化**
  - React.memo：组件缓存
  - useMemo/useCallback：避免不必要的渲染
  - 懒加载（React.lazy）

#### 3. 浏览器相关
- **DOM 操作**
  - 查询元素：querySelector/getElementById
  - 事件处理：addEventListener
  - 事件委托

- **浏览器 API**
  - IntersectionObserver（你的项目用了）
  - requestAnimationFrame
  - localStorage/sessionStorage

---

### ⭐⭐ 需要了解（可能问到）

#### 1. CSS
- Flexbox 布局
- Grid 布局
- 响应式设计（媒体查询）
- CSS Modules / SCSS

#### 2. 工程化基础
- Webpack 基本概念（入口、输出、loader）
- npm/yarn 包管理
- 环境变量配置

#### 3. 网络请求
- fetch API
- axios 使用
- 错误处理

---

### ⭐ 有时间再看（加分项）

- TypeScript 基础
- 性能优化指标（FCP、LCP）
- 虚拟列表原理
- 服务端渲染（SSR）概念

---

## 💼 三、项目讲解策略（诚实但有效）

### 项目背景说明（建议这样说）

> "这是一个基于React开发的TikTok风格视频流媒体平台前端项目。我在学习React的过程中，参考了一些开源项目，然后自己实现了核心功能，包括视频Feed流、无限滚动、全屏浏览等。"

### 核心功能讲解（重点准备）

#### 1. 无限滚动实现
**技术点**：IntersectionObserver API

**你的代码位置**：`src/hooks/useInfiniteScroll.js`

**讲解要点**：
- 使用 IntersectionObserver 监听底部元素
- 当元素进入视口时，自动触发加载更多
- 实现了防抖机制，避免频繁请求
- 添加了错误重试机制

**面试官可能问**：
- Q: 为什么用 IntersectionObserver 而不是监听 scroll 事件？
- A: IntersectionObserver 性能更好，不会阻塞主线程，而且可以精确控制触发时机。

#### 2. 虚拟窗口机制（如果问到）
**技术点**：性能优化、DOM节点控制

**你的代码位置**：`src/components/VideoFeed/VideoFeed.js` (182-209行)

**讲解要点**：
- 当视频列表很长时，只渲染可见区域附近的视频
- 使用双向窗口策略：以当前可见视频为中心，保留前后各N个视频
- 通过 IntersectionObserver 跟踪当前可见的视频索引
- 动态更新窗口范围，减少DOM节点数量

**面试官可能问**：
- Q: 虚拟列表和虚拟窗口有什么区别？
- A: 虚拟列表通常用于固定高度的列表，虚拟窗口更适合动态高度的内容（如视频卡片）。

#### 3. 自定义 Hooks
**技术点**：React Hooks、代码复用

**你的代码位置**：`src/hooks/`

**讲解要点**：
- useInfiniteScroll：无限滚动逻辑封装
- useFullscreenNavigation：全屏模式导航管理
- useSwipeGesture：手势滑动处理
- 通过自定义Hooks实现逻辑复用

**面试官可能问**：
- Q: 为什么要封装成自定义Hook？
- A: 提高代码复用性，将业务逻辑和UI分离，便于测试和维护。

#### 4. 状态管理
**技术点**：useReducer、复杂状态管理

**你的代码位置**：`src/components/VideoFeed/VideoFeed.js` (13-59行)

**讲解要点**：
- 使用 useReducer 管理分页状态（loading、hasMore、error等）
- 通过 action 统一管理状态变更
- 比多个 useState 更适合复杂状态场景

---

### 项目难点和解决方案（准备1-2个）

**示例1：视频自动播放控制**
- **问题**：如何实现视频进入视口自动播放，离开视口自动暂停？
- **解决方案**：使用 IntersectionObserver 监听视频元素，根据 intersectionRatio 判断是否播放
- **代码位置**：`src/components/VideoItem/VideoCard/VideoCard.js` (220-263行)

**示例2：滚动位置恢复**
- **问题**：用户从详情页返回列表页时，如何恢复之前的滚动位置？
- **解决方案**：使用 localStorage 保存滚动位置，页面加载时恢复
- **代码位置**：`src/App.js` (9-42行)

---

## 📝 四、常见面试题准备

### JavaScript 基础题

#### 1. 闭包的应用场景
```javascript
// 防抖函数
function debounce(func, delay) {
  let timer;
  return function(...args) {
    clearTimeout(timer);
    timer = setTimeout(() => func.apply(this, args), delay);
  };
}
```

#### 2. 数组去重
```javascript
// 方法1：Set
const unique = [...new Set(arr)];

// 方法2：filter + indexOf
const unique = arr.filter((item, index) => arr.indexOf(item) === index);
```

#### 3. 深拷贝
```javascript
// 简单版本（不处理函数、Date等）
const deepClone = (obj) => {
  if (obj === null || typeof obj !== 'object') return obj;
  if (obj instanceof Date) return new Date(obj);
  if (obj instanceof Array) return obj.map(item => deepClone(item));
  return Object.keys(obj).reduce((acc, key) => {
    acc[key] = deepClone(obj[key]);
    return acc;
  }, {});
};
```

### React 基础题

#### 1. useEffect 的依赖数组
```javascript
// 空数组：只在组件挂载时执行一次
useEffect(() => {
  // ...
}, []);

// 有依赖：依赖变化时执行
useEffect(() => {
  // ...
}, [count]);

// 无依赖：每次渲染都执行（不推荐）
useEffect(() => {
  // ...
});
```

#### 2. useState 和 useReducer 的区别
- useState：适合简单状态
- useReducer：适合复杂状态，多个相关状态需要统一管理

#### 3. 如何避免不必要的渲染？
- React.memo：组件级别缓存
- useMemo：值缓存
- useCallback：函数缓存

### 算法题（简单级别）

#### 1. 两数之和
```javascript
function twoSum(nums, target) {
  const map = new Map();
  for (let i = 0; i < nums.length; i++) {
    const complement = target - nums[i];
    if (map.has(complement)) {
      return [map.get(complement), i];
    }
    map.set(nums[i], i);
  }
  return [];
}
```

#### 2. 反转字符串
```javascript
function reverseString(str) {
  return str.split('').reverse().join('');
}
```

---

## 🚨 五、应急策略（如果不会）

### 遇到不会的问题时

1. **诚实回答**
   > "这个问题我之前没有深入了解过，但我可以基于我的理解尝试回答..."

2. **展示学习能力**
   > "如果让我来解决这个问题，我会先查阅文档，然后通过实践来验证..."

3. **关联已有知识**
   > "这个问题让我想到了之前项目中遇到的XXX问题，当时我是这样解决的..."

### 常见应急话术

- **关于新技术**：
  > "虽然我没有实际使用过，但我了解它的基本概念，如果有机会我会快速学习并应用到项目中。"

- **关于复杂问题**：
  > "这个问题比较复杂，我需要一些时间思考。能否给我一点时间，或者我们可以先讨论一下解决思路？"

---

## 📋 六、面试前检查清单

### 技术准备
- [ ] 复习 JavaScript 核心概念（闭包、this、异步）
- [ ] 复习 React Hooks（useState、useEffect、useCallback）
- [ ] 准备项目讲解（3-5分钟）
- [ ] 准备1-2个项目难点和解决方案
- [ ] 练习常见算法题（简单级别）

### 项目准备
- [ ] 熟悉项目代码结构
- [ ] 能解释核心功能的实现原理
- [ ] 准备项目演示（如果有时间）
- [ ] 思考项目的优化方向

### 产品了解
- [ ] 访问 Wegic 官网，体验产品
- [ ] 了解产品的主要功能
- [ ] 思考：如果加入团队，我能贡献什么？

### 心态准备
- [ ] 保持自信，但不过分夸大
- [ ] 诚实回答，展示学习能力
- [ ] 准备提问环节的问题（如：团队技术栈、项目情况、成长机会）

---

## 💡 七、加分项准备

### 如果时间充裕，可以准备：

1. **性能优化知识**
   - 了解 FCP、LCP 等性能指标
   - 知道如何优化首屏加载时间

2. **工程化知识**
   - Webpack 基本配置
   - 代码分割（Code Splitting）

3. **现代前端趋势**
   - 了解 React 18 新特性
   - 了解前端工程化趋势

---

## 🎯 八、面试当天建议

### 面试前1小时
- 再次浏览项目代码
- 复习核心概念
- 准备自我介绍（1-2分钟）

### 面试中
- 听清楚问题再回答
- 不会的问题诚实回答，展示学习能力
- 主动展示项目亮点

### 面试后
- 记录面试问题，查漏补缺
- 发送感谢邮件（可选）

---

## 📚 九、快速复习资源

### JavaScript
- MDN Web Docs：https://developer.mozilla.org/zh-CN/
- JavaScript.info：https://zh.javascript.info/

### React
- React 官方文档：https://react.dev/
- React Hooks 详解：https://react.dev/reference/react

### 算法练习
- LeetCode 简单题：数组、字符串相关
- 重点：两数之和、反转字符串、数组去重

---

## ⚠️ 十、注意事项

1. **诚实原则**：不要夸大项目经验，但可以强调学习能力和解决问题的思路
2. **项目讲解**：重点讲你理解的部分，不要讲你不懂的技术细节
3. **保持自信**：即使有些问题不会，也要展示学习能力和解决问题的思路
4. **提问环节**：准备2-3个问题，如：团队技术栈、项目情况、成长机会

---

## 🎉 最后的话

面试不仅是考察技术能力，也是考察学习能力、沟通能力和团队协作能力。即使有些技术点不熟悉，也要展示你的学习能力和解决问题的思路。

**记住**：
- ✅ 诚实 > 夸大
- ✅ 学习能力 > 现有知识
- ✅ 解决问题思路 > 完美答案

**祝你面试顺利！** 🚀







